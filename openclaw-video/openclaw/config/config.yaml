{
  gateway: {
    mode: "local",
    bind: "lan",
    port: 18789,
    auth: {
      mode: "token",
    },
    controlUi: {
      enabled: false,
    },
  },
  agents: {
    defaults: {
      model: {
        primary: "volcengine-plan/ark-code-latest",
      },
    },
  },
}
