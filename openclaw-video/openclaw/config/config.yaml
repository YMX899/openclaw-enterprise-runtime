{
  gateway: {
    mode: "local",
    bind: "lan",
    port: 18789,
    auth: {
      mode: "token",
    },
    controlUi: {
      enabled: false,
    },
  },
  agents: {
    defaults: {
      model: {
        primary: "volcengine-plan/ark-code-latest",
      },
      models: {
        "volcengine-plan/ark-code-latest": {
          params: {
            maxTokens: 32768,
          },
        },
      },
    },
  },
}
